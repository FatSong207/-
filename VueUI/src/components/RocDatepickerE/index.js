import DatePickerE from './src/picker/date-picker'

/* istanbul ignore next */
DatePickerE.install = function install(Vue) {
  Vue.component(DatePickerE.name, DatePickerE)
}

export default DatePickerE
